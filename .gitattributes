# PowerShell scripts for Windows compatibility
# install.ps1 must NOT have BOM (piped execution via irm|iex)
install.ps1 text eol=crlf
# Other .ps1 files need UTF-8 BOM for Windows Chinese systems
ai-cli.ps1 text eol=crlf working-tree-encoding=UTF-8

# Ensure line endings
*.md text eol=lf
*.json text eol=lf
*.sh text eol=lf
